#include <stdio.h>

int main()
{
    char *p = "I am a string";
    char *q = "I am a string";
v
    if (p == q)
    {
        printf("Optimized");
    }
    else
    {
        printf("Change your compiler");
    }
    return 0;
}